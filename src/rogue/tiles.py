"""地图格子的字符常量（M5 起由 game.py / level.py / fov.py 共用，避免字面量各写一份）。

只是常量，无逻辑、无随机。
"""
WALL = "#"      # 墙：不可通行
FLOOR = "."     # 地板：可通行
PLAYER = "@"    # 玩家（蜘蛛侠）：grid 上唯一，实体层渲染时也用它
MONSTER = "M"   # 怪物（渲染层字符，不写入 grid）
ITEM = "!"      # 地面道具（渲染层字符，不写入 grid）
STAIRS = ">"    # 下行楼梯（渲染层字符，不写入 grid）

# M6 视野 / 迷雾
UNSEEN = " "    # 未探索区域（只在开启视野时用到）
SENSE = "?"     # 蜘蛛感应：看不见、但能感知到的威胁轮廓

# M7 潜行
UNAWARE = "m"   # 未察觉玩家的敌人（还在巡逻/发呆，可倒挂突袭）
