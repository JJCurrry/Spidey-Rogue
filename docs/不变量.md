# 不变量（红线，≤20 条，机器可判定优先）

> 这些是 L1 墙 `seed-guard` 会扫描拦截的硬约束。新增红线请同步改 `scripts/gate.py`。

## 当前生效
1. **随机必须 Seed 注入**：游戏内一切随机（掉落/生成/AI）必须经由 `src/rogue/rng.py` 的 `RandomSource`；禁止在 `src/` 其它文件裸调 `random`/`secrets`/`os.urandom`/`randint`。目的：测试可重现（对应 demo 的 Clock 红线）。
2. **回合确定性**：相同 seed + 相同输入序列 ⇒ 相同结果。任何破坏确定性的隐藏随机源都禁止。
3. **玩家 HP 永不为负**（M2 起生效）：`hp = max(0, hp)` 由引擎保证，业务不得直接写负。
4. **不可越界 / 不可穿墙**：移动必须经过 `Game.move` 的边界与墙检查（M1 GB-1）。道具生成亦同：`spawn_item` 对墙格/越界格返回 `None`（M4）。
5. **背包容量上限（M4 起生效）**：`len(Game.inventory) <= INVENTORY_CAPACITY(=5)`；满包时 `pick_up()` 返回 `None` 且道具**留在地面**，不做自动替换/丢弃。机器判定：`tests/test_items.py::TestInventoryCapacity`。
6. **玩家 HP 不超过上限（M4 起生效）**：一切治疗必须经 `Game._heal_player` 的 `min(max_hp, ...)` 钳制。机器判定：`tests/test_items.py::TestSandwich`。
7. **地图连通性（M5 起生效）**：程序化楼层中，玩家起点经四方向洪泛可达**全部**可通行格。
   - 生成期保证：房间按生成顺序首尾相连成链（L 形走廊），链上必连通；
   - 生成后兜底：`level._seal_unreachable` 把不可达的死口袋回填成墙，故该不变量恒真；
   - 机器判定：`tests/test_level.py::TestConnectivity`（30 个 seed × 不同 depth），自检接口 `Level.is_connected()`。

8. **视野/渲染纯净性（M6 起生效）**：`render()` 只依赖地形 + 实体 + 玩家位置；
   禁止裸随机（沿用 #1），且**不得改写** `grid` / 玩家 / 怪物 / 道具的任何状态。
   唯一例外是「已探索记忆」集合 `explored` 单调增长（`explored |= visible`），
   它不影响任何玩法判定（#2 的随机序列、#3 的 HP、#5 的背包都不受影响）。
   - 视野本身是**纯几何**计算（`src/rogue/fov.py`：Bresenham 射线 + 半径 + 房间照明），
     不消耗 `RandomSource` ⇒ 开不开视野，战斗/掉落/生成的随机序列完全一致；
   - 机器判定：`tests/test_fov.py::TestRenderPurity`（world state 前后快照比对）
     与 `::TestFovDeterminism`（同 seed + 同输入 ⇒ 同画面；开视野不消耗随机）。

9. **感知 / 潜行确定性（M7 起生效）**：怪物「有没有发现玩家」是**纯几何**判定
   （`src/rogue/fov.py::monster_can_see`：双向 Bresenham 视线 + 感知半径 + 同房间互通），
   **不消耗 `RandomSource`**；潜行**默认关闭**（`stealth=False` ⇒ `Monster.alerted` 恒为 True，
   怪物仍是 M3 的全知追击）。
   - 倒挂突袭只由 `Monster.alerted` 决定，额外伤害是**确定性倍率** `SNEAK_ATTACK_MULT=2`，
     不额外掷骰 ⇒ 不扰动战斗/掉落/生成的随机序列（#2）；
   - 双向视线的硬性质：**怪物看得见你 ⇒ 你一定看得见它**（怪物感知半径 7 < 玩家视野 8）
     ——不存在「看不见的敌人在追你」；
   - 机器判定：`tests/test_stealth.py::TestStealthDeterminism`（rng 序列对照 + 同 seed 重放）
     与 `::TestMonsterSight::test_anything_that_sees_you_you_can_see`。

10. **噪音 / 听觉确定性（M8 起生效）**：声音传播是**纯几何**判定
    （`src/rogue/sound.py::noise_field`：在 `grid` 上跑最短传播代价，空地 1 / 墙 3），
    **不消耗 `RandomSource`**；听觉**默认关闭**（`noise=False` ⇒ 只有视觉一条感知通道）。
   - 「是否被听到」只经 `Monster.alert(..., cause=CAUSE_SOUND)` 这一**唯一入口**影响 `alerted`，
     不额外掷骰 ⇒ 不扰动战斗/掉落/生成的随机序列（#2）；也不碰 `grid` / HP / 背包（#3/#5/#6）；
   - 响度是常量表（`NOISE_PUNCH=6` / `NOISE_SNEAK=2` / `NOISE_STRUGGLE=7` / `NOISE_LANDING=8`），
     **声源未必是玩家**（被蛛网弹缠住的怪自己会挣扎出声）⇒ 怪物会扑向声源、可能被误导；
   - 视听同时发生时**以视觉为准**（视觉给实时位置，听觉只给声源）；
   - 机器判定：`tests/test_noise.py::TestNoiseDeterminism`（rng 序列对照 + 同 seed 重放）
     与 `::TestNoiseSources`（四个声源 + 声源误导）。

11. **主动制造响动 / 投掷确定性（M9 起生效）**：投掷是纯几何判定
    （`fov.has_line_of_sight` + 射程 + 落点可通行），不消耗 RandomSource；
    诱饵只在听觉开启时出现（noise=False ⇒ 既不刷「皇后区垃圾桶盖」、也甩不响，M1~M8 一字节不变）。
   - 声源由玩家指定（`throw_decoy` 落点），不是由战斗位置决定 ⇒ 调虎离山成为主动选择；
   - 机器判定：`tests/test_decoy.py::TestDecoyGeometry` 与 `::TestDecoyDeterminism`。

12. **光照衰减（M11 起生效）**：光照是纯几何、不消耗 RandomSource；光照默认关闭
    （light=False ⇒ 怪物感知半径恒满、render() 字形一字不差，M1~M10 一字节不变）；
    暗处只缩短怪物感知半径（恒 ≤ MONSTER_SIGHT_RADIUS），所以「怪看得见你 ⇒ 你看得见它」的硬性质不破。
   - 光源 = 房间中心固定灯 + 玩家随身微光；光遇墙即断、随距离线性衰减；每格落暗/昏暗/明三档；
   - 明暗梯度只在展示层 `colorize` 上色，不改 `render()` 字形（不变量 #8 延伸）；
   - 机器判定：`tests/test_light.py::TestMonsterSightDecay` 与 `::TestLightSymmetry` 与 `::TestRenderPurity`。

13. **随身手电动态光源（M12 起生效）**：玩家可开关的随身光，纯几何、不消耗 RandomSource；
    默认不装备（flashlight=False ⇒ 即使 light=True 也与 M11 逐字节一致）；开启时只在玩家处追加一个
    半径 FLASHLIGHT_RADIUS 的光源，同样只缩短怪物感知（半径恒 ≤ MONSTER_SIGHT_RADIUS），
    对称硬性质不破；`toggle_flashlight` 只翻标志 + 重算光照场，不改写任何玩法状态（换层保留）。
   - 机器判定：`tests/test_flashlight.py::TestFlashlightSources` 与 `::TestFlashlightRenderPurity`
     与 `::TestFlashlightDeterminism`。

14. **光照影响玩家自身视野（M13 起生效）**：光照开启且视野开启时（`light=True` 且 `fov=True`），
    玩家视野半径按**目标格光照**衰减（暗 2 / 昏暗 4 / 明 8）——亮处的东西容易被看见、暗处的东西难被看见；
    纯几何、零随机、不消耗 `RandomSource`；光照关闭或视野关闭 ⇒ 走 M6 原逻辑（固定半径 8），M1~M12 一字节不变。
   - 按目标格光照算（不是玩家所在格）：玩家微光让脚下恒 LIT，按玩家所在格算会退化为「视野永远 8」；
     按目标格算则「走廊暗处看不远、房间亮处看得清」，与「暗处近视」的直觉一致；
   - 有效半径恒 ≤ `SIGHT_RADIUS`(8)（只缩短不放大），且同光照等级下 `player_r ≥ monster_r`
     （DARK 2=2 / DIM 4=4 / LIT 8>7）⇒ #9 硬性质「怪看得见你 ⇒ 你看得见它」不破
     （怪物感知取决于怪物所在格光照、玩家视野取决于目标格=怪物所在格光照，同一格 ⇒ 档位对应）；
   - `render()` 字形一字不差（可见集合变小但不改字形，#8 延伸）；进房间点亮整间规则保留
     （房间内格子 LIT ⇒ 半径 8，与 M6 原逻辑一致）；
   - 机器判定：`tests/test_light_fov.py::TestPlayerSightRadius` 与 `::TestVisibleTilesLightField`
     与 `::TestLightFovSymmetry` 与 `::TestLightFovDeterminism` 与 `::TestLightFovRenderPurity`。

15. **可开关房间灯（M14 起生效）**：蛛网射中房间中心灯的拉链，翻转该房间的灯（关灯变暗 / 开灯恢复）；
    纯几何、零随机、不消耗 `RandomSource`；默认关闭（`light=False` ⇒ `toggle_light` 恒返回 False、no-op，
    与 M1~M13 逐字节一致）。
   - 关灯只移除光源（`_light_sources` 跳过 `switched_lights` 里的房间中心），不新增光源
     ⇒ 光照场只变暗或恢复，有效半径恒 ≤ `SIGHT_RADIUS`(8) ⇒ #9/#12/#14 对称硬性质不破；
   - 四条硬约束（任一不满足即够不着）：光照开启 + 目标是房间中心 + 切比雪夫距离 ≤ `WEB_LIGHT_RANGE`(6)
     （含脚下——站在灯正下方伸手就能够到）+ 玩家看得见灯（`has_line_of_sight`）；
   - M8 联动：拉链轻响 `NOISE_TOGGLE_LIGHT=3` 从**灯处**传出（不在玩家处）⇒ 调虎离山成立
     （比垃圾盖 `NOISE_DECOY=9` 轻、只惊动近处的人，但同时把房间弄暗）；
   - `toggle_light` 只翻 `switched_lights` 集合 + 重算光照场/可见集合（`update_fov`），
     不改写 `grid`/HP/背包/怪物状态（与 `toggle_flashlight` 同一零副作用哲学）；换层（`load_level`）重置；
   - `render()` 字形一字不差（关灯只改光照场与可见集合，不改字形，#8 延伸）；
   - 机器判定：`tests/test_light_switch.py::TestToggleLightGeometry` 与 `::TestToggleLightField`
     与 `::TestToggleLightDeterminism` 与 `::TestToggleLightNoise` 与 `::TestToggleLightRenderPurity`
     与 `::TestToggleLightSymmetry` 与 `::TestToggleLightDefault` 与 `::TestToggleLightReset`。

16. **可破坏的灯（M16 起生效）**：蛛网射碎房间中心灯泡，一次性不可逆（per floor）；纯几何、零随机、不消耗 `RandomSource`；默认关闭
    （light=False ⇒ `destroy_light` 恒返回 False、no-op，与 M1~M15 逐字节一致）。
   - 破坏只移除光源（`_light_sources` 同时跳过 `switched_lights` 与 `destroyed_lights` 里的房间中心，与翻转开关合并），
     不新增光源 ⇒ 光照场只变暗、有效半径恒 ≤ `SIGHT_RADIUS`(8) ⇒ #9/#12/#14 对称硬性质不破；
   - 五条硬约束（任一不满足即够不着）：光照开启 + 目标是房间中心 + 切比雪夫距离 ≤ `WEB_LIGHT_RANGE`(6)
     （含脚下）+ 玩家看得见灯（`has_line_of_sight`）+ 该灯**尚未被破坏**；
   - 已破坏的灯**不再可 toggle**（`can_toggle_light` 对已破坏者返回 False）；破坏也可作用于「已关灯」（switched）的房间

17. **环境场与完整场分离（M17 起生效）**：环境光照场（`ambient_field`，仅房间中心固定灯，不含玩家微光/随身手电）
    与完整光照场（`light_field`，房间灯 + 玩家微光 + 随身手电）由同一 `update_light()` 纯几何、零随机、幂等重算；
    默认所有玩法/渲染判定走完整场（`light_level_at` / `light_field`），环境场仅作「只看静态环境照明」的只读查询入口
    （`ambient_level_at`）对外暴露 ⇒ 行为零回归（gate 432 → 443，演示逐字节一致）。
   - 两份场同源：都跳过 `switched_lights` / `destroyed_lights`（关灯/碎灯同时作用于两份场，只移除不新增）⇒ 光照场只变暗不变亮 ⇒ #9/#12/#14 不破；
   - `light=False` 时两份场皆清空、`ambient_level_at` / `light_level_at` 恒返回「明亮」，M1~M16 一字节不变；
   - 任何「改用环境场」的玩法改动（如怪物感知改用环境场）是后续 opt-in 项，**不在 M17 默认开启**，需重跑 `--light`/`--flashlight` 演示确认平衡。
     ⇒ 永久黑暗、不可恢复；
   - M8 联动：碎裂轻响 `NOISE_SHATTER_BULB=3` 从**灯处**传出（不在玩家处）⇒ 调虎离山成立
     （与 M14 拉链轻响同值、同原则）；
   - `destroy_light` 只翻 `destroyed_lights` 集合 + 重算光照场/可见集合（`update_fov`），
     不改写 `grid`/HP/背包/怪物状态（与 `toggle_light`/`toggle_flashlight` 同一零副作用哲学）；换层（`load_level`）重置；
   - `render()` 字形一字不差（破坏只改光照场与可见集合，不改字形，#8 延伸）；
   - 机器判定：`tests/test_destructible_light.py::TestDestroyGeometry` 与 `::TestDestroyField`
     与 `::TestDestroyDeterminism` 与 `::TestDestroyNoise` 与 `::TestDestroyRenderPurity`
     与 `::TestDestroySymmetry` 与 `::TestDestroyDefault` 与 `::TestDestroyReset`
     与 `::TestDestroyInteractionWithToggle` 与 `::TestDestroyQuery` 与 `::TestDestroyMultipleRooms`。

18. **怪物感知改用环境场（M18 起生效）**：M11 怪物感知半径判定改走「怪物感知光场」——**仅房间灯 + 随身手电，不含玩家被动微光**
    （`Game.monster_light_level_at`，由 `update_light()` 与 `light_field`/`ambient_field` 同源、幂等重算），
    修正 M11 的隐性副作用：玩家被动微光原会照亮附近暗处的怪、使怪物感知半径不被缩短（等于把近视眼照成远视眼）；
    现在被动微光不再替暗处的怪点灯 ⇒ 暗处怪仍是近视眼、你更难被发现。
   - 纯几何、零随机（不变量 #1/#2）：`monster_light_level_at` 不消耗 `RandomSource`，`light=False` 时恒返回「明亮」；
   - 只缩短不放大：怪物感知光场只含「房间灯 + 手电」、不含被动微光，有效半径恒 ≤ `MONSTER_SIGHT_RADIUS`(7)
     ⇒ #9 硬性质「怪看得见你 ⇒ 你看得见它」不破（与 M12 手电双刃并存：你开手电仍会让自己更易被暗处怪看见）；
   - 关灯 / 碎灯同时作用于 `monster_light_field`（与 `light_field`/`ambient_field` 一起跳过 `switched_lights`/`destroyed_lights`）
     ⇒ 只移除光源、不新增 ⇒ #12/#14 不破；
   - `light=False` 时怪物感知与 M1~M17 逐字节一致（被动微光只在 light=True 才有意义）；
   - 渲染梯度（colorize 明暗上色）仍走完整场 `light_field`，不受此影响（#8 渲染纯净性延伸）；
   - 机器判定：`tests/test_ambient_light.py::TestMonsterPerceptionUsesAmbientExcludingGlow`
     与 `tests/test_light.py::TestLightAwareness::test_passive_glow_does_not_light_monster`
     与 `tests/test_flashlight.py::TestFlashlightPerception::test_flashlight_makes_distant_monster_see_you`。

## 待生效（后续里程碑补机器检查）
- 怪物属性非负。
- 道具属性非负。

## 怪行为登记
- M3 怪物行为（详见 `docs/工单/T-003-怪物AI.md`）：
  - `chase`（贪心追击）：曼哈顿距离逼近玩家；**完全不消耗 rng**，保持纯确定性（不变量 #2）。
  - `wander`（随机游走）：方向选择经 `self.rng.chance` / `self.rng.choice`（不变量 #1）；`chase` 行为不调 rng。
  - 相邻（切比雪夫距离 1）怪物反击玩家，固定伤害，经 `Game._hurt_player` 钳制 HP ≥ 0（不变量 #3）。
  - 移动经 `_monster_can_enter` 把关：越界/墙/玩家格/其它怪物格均不可入（不变量 #4）。

## 道具登记（M4）
- 掉落判定 `rng.chance(DROP_PROB)` → 种类 `rng.choice(ITEM_KEYS)`，均经 `self.rng`（不变量 #1）；只在「怪物阵亡」这一确定性事件上掷骰（不变量 #2）。
- 效果（详见 `docs/工单/T-004-道具与背包.md`）：
  - `sandwich` 梅姨的三明治：回血，经 `_heal_player` 钳制（不变量 #6）；满血时不消耗。
  - `web_cartridge` 蛛网发射器备用芯：对最近存活怪 5 点伤害 + `stunned=1`。
  - `nano_boost` 斯塔克纳米强化剂：`player_dmg_bonus += 2`（本局内累加）。
- 束缚：`Monster.stunned > 0` 时 `monster_act` 直接消耗一层并返回，**不移动、不攻击、不消耗 rng**（保持 #2 纯确定性）。
- 一格只放一个道具：`spawn_item` 遇到该格已有道具返回 `None`。

## 视野登记（M6）
- 视野在 `src/rogue/fov.py`：Bresenham 射线判遮挡 + 半径 `SIGHT_RADIUS=8` + 「进房间点亮整间」（复用 `Game.rooms`）；**纯几何、零随机**（不变量 #1/#2）。
- 蜘蛛感应 `SPIDER_SENSE_RADIUS=4`（切比雪夫、**穿墙**）：看不见的怪物画成 `?`，只给轮廓不给种类；半径刻意小于视野，是预警不是透视。
- 视野是**可选开关**：`Game(..., fov=False)` 默认关闭 ⇒ `render()` 仍是全图（M1~M5 的既有规格都跑在这条路径上）；`fov=True` 才走迷雾渲染。
- 渲染优先级（后者覆盖前者）：蜘蛛感应 `?` < 楼梯 `>` < 道具 `!` < 怪物 `M` < 玩家 `@`（沿用 M5 的 楼梯<道具<怪物<玩家）。
- 记忆规则：地形 / 楼梯 / 道具一旦看见就留在记忆里（它们不动）；**怪物只在当前可见时绘制**（怪物会跑，记忆会骗人）。
- 换层即失忆：`load_level` 清空 `explored`（上一层的记忆对新楼层无意义）。

## 怪物感知与潜行登记（M7）
- 感知在 `src/rogue/fov.py::monster_can_see`：**纯几何、零随机**（不变量 #1/#2/#9），
  四条判定依次短路：同格 / 同一房间 / 超出 `MONSTER_SIGHT_RADIUS=7` / **双向**射线判遮挡。
  半径刻意小于玩家视野（8）⇒ 玩家总能先发现敌人。
- 视线必须**双向**都通（不只判单向）：整数 Bresenham 不对称，(1,1)→(3,2) 途经 (2,1)、
  而 (3,2)→(1,1) 途经 (2,2)；单向判定会造出「隔着拐角看得见你、你却看不见它」的幽灵猎手。
- 感知只在**世界回合开始时**更新一次（`monster_turn` → `update_awareness`）。这是潜行成立的
  关键：玩家在自己回合从敌人视野外扑到面前时，敌人还停留在「未察觉」状态。
- 警觉状态机：`alerted` / `alert_turns(=ALERT_MEMORY=3)` / `last_seen` / `home`。
  被惊动后扑向 `last_seen`（不是全知追踪）；看不见了就每回合递减，归零即放弃、回巢待命。
  未察觉的 `chase` 怪回巢（确定性、不消耗 rng）；未察觉的 `wander` 怪照旧随机游走、偶尔想起回巢。
- 倒挂突袭：攻击 `alerted=False` 的敌人 ⇒ 伤害 × `SNEAK_ATTACK_MULT=2`（确定性常数）、
  **不挨反击**；命中后该敌立刻转「已察觉」，下回合照常还手。
  入口 `Game.web_strike(target)`：荡到目标身旁并立刻出手，射程 `WEB_STRIKE_RANGE=2` 步
  （沿可通行格 BFS，不穿墙，不变量 #4 成立）。
- 潜行**默认关闭**：`Game(..., stealth=False)` ⇒ 怪物恒为「已察觉」，M1~M6 的既有规格零改动。
- 画面：未察觉的敌人画小写 `m`、已察觉的画 `M`（两条渲染路径都区分；潜行关闭时全是 `M`
  ⇒ 既有 `assertIn("M", render())` 断言仍成立）。
- 已修（M15）：M3 的老追击路径 `_step_toward` 改用切比雪夫二次关键字（close_in=True），
  与潜行分支统一——玩家沿轴向往复时怪不再锁轴震荡、能稳定贴上。修法纯几何、零随机（#2 不变）。
  因威胁上升，怪物攻击值各降 1（1~3 → 0~2）配平：只修不配平会从 26/30 掉到 22/30，配平后回到 ~28/30。

## 噪音与听觉登记（M8）
- 传播在 `src/rogue/sound.py`：**纯几何、零随机**（不变量 #1/#2/#10）。
  `noise_field` 以声源为起点跑 Dijkstra，踏入空地代价 1、踏入墙代价 3，超过响度的格子不入場；
  声源自身代价 0。**越界格不参与传播**，声源在界外 ⇒ 谁也听不见。
- 声音**绕着走**：与光不同（光遇墙即断），声音沿走廊传播，三格厚的墙不如绕下面的走廊便宜
  ⇒ 拐角后面的房间听不见，但一墙之隔只是「闷」（穿墙白丢 2 格传播距离）。
- 听觉**默认关闭**：`Game(..., noise=False)` ⇒ 只有视觉一条感知通道，M1~M7 的既有规格零改动。
  与 `fov` / `stealth` 同一哲学：新能力显式 opt-in。
- 四个声源（只有**动作**会响，走路 / 拾取 / 吃三明治 / 注射纳米强化剂全无声）：
  蛛网拳 `NOISE_PUNCH=6`（玩家处）、倒挂突袭 `NOISE_SNEAK=2`（玩家处，几乎无声）、
  蛛网弹命中 `NOISE_STRUGGLE=7`（**被缠住的怪处**，它活着才挣扎）、下潜落地 `NOISE_LANDING=8`（新层起点）。
  落地声只在 `descend()` 里发（开局你已经在楼里了），不在 `load_level`。
- `Game.emit_noise(x, y, loudness)` 惊动听得见的存活怪，`last_seen = 声源`（不是玩家位置）⇒
  声源不在你脚下时它们会被误导（调虎离山）。听觉关闭时该函数是空操作、返回空表。
- `Monster.alert_cause`：`"sight"` / `"sound"` / `None`。**被直接命中的怪记成「看见」**，
  只有听见动静的才记成「听见」⇒ 画面上 `~` 只出现在「它还没看见你」的敌人身上。
- 画面：未察觉 `m` ｜ 听见动静但还没看见你 `~` ｜ 已看见你 `M`；渲染优先级沿用 M6/M7
  （蜘蛛感应 `?` < 楼梯 `>` < 道具 `!` < 怪物 `m`/`~`/`M` < 玩家 `@`）。

## 楼层生成登记（M5）
- 地形生成在 `src/rogue/level.py`，随机只来自注入的 `RandomSource`（不变量 #1）；`level.py` 与 `game.py` 都不直接引入随机模块（seed-guard 拦截）。
- 生成流程：拒绝采样摆房间 → 按生成顺序连成链（L 形走廊，拐向经 `rng.chance`）→ 额外环路 → 定起点与楼梯 → 洪泛封填死口袋（不变量 #7）。
- 楼层尺寸 33×17、房间 4~8 × 3~5、最多 7 间；外圈恒为墙（不变量 #4）。
- 楼梯是**坐标属性** `Game.stairs`，**不写进 `grid`**（与道具同属实体层渲染，见假设 C/E）；玩家离开楼梯格后 `grid` 恢复为地板，楼梯仍在原处。
- 下潜：`can_descend()` 仅在站上楼梯时为真；`descend()` 重生成下一层、重置地图与实体，**保留**玩家 HP / 背包 / 纳米加成（M2/M4 状态跨层继承）。
- 撒点：非起始房间以 `MONSTER_ROOM_PROB` 判定「有没有怪」、再刷 1~2 只；整层保底 1 只；道具按 `ITEM_ROOM_PROB` 撒，复用 M4 的 `spawn_item`（不变量 #4/#5）。
